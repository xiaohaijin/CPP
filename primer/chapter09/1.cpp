/*!
对于下面的程序任务，vector, deque和list哪种容器最为合适？
(1) 读取固定数量的单词，将它们按字典顺序插入到容器中。
(2) 读取未知数量的单词，总是将新单词插入到尾部。删除操作在头部进行。
(3) 从一个文件读取位置数量的整数， 将这些数排序，然后将他们打印到标准
输出。

anwser: 
(1) : list
(2) : deque
(3) : vector
*/
